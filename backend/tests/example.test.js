 import { describe, it, expect } from 'vitest'

describe('Math', () => {
  it('adds numbers', () => {
    expect(2 + 2).toBe(4)
  })

  it('multiplies numbers', () => {
    expect(3 * 2).toBe(6)
  })
})